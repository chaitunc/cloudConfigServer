spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chaitunc/config-repo.git
        
  profiles:
     active: local

server:
  port: 8888

security:
  user:
    password: test
